@model IEnumerable<DHC.DAL.Models.ModelCarName>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ModelName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ModelName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ModelNameId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ModelNameId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ModelNameId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
